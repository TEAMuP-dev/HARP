# Known-good backend (isolated Python 3.9, CPU) install set for sony/FxNorm-automix.
# Dual-interpreter Docker Space. torch is the CPU wheel; the native augmentation
# libs (aubio/sox/soxbindings) are STUBBED in the worker (unused on the mixing
# inference path), so this stays a pure-wheel install. psutil was undeclared but
# imported by automix.common_miscellaneous -- added after tracing the import chain.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==1.9.0+cpu
numpy==1.23.5
scipy==1.10.1
numba==0.56.4
llvmlite==0.39.1
librosa==0.9.2
resampy==0.4.2
soundfile>=0.12.1
scikit-learn==1.1.3
pyloudnorm==0.1.1
psutil==5.9.8
git+https://github.com/csteinmetz1/pymixconsole
sty
git+https://github.com/sony/FxNorm-automix
