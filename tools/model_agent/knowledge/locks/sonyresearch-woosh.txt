# Known-good backend install set for SonyResearch/Woosh (Woosh-DFlow), from the
# hand-authored Docker backend (Python 3.12). This is NOT a pyharp Space: it is
# the model-running half of a two-Space deploy (backend Docker + pyharp remote
# frontend). torch is installed from the pytorch CPU wheel index; hear21passt and
# the woosh package itself are installed --no-deps so pip never re-resolves torch.
# ~3.4 GB of CC-BY-NC weights are baked in from the v1.0.0 GitHub release zips.
--index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
einops>=0.8.1
hydra-core>=1.3.2
lightning>=2.5.6
timm>=1.0.22
torchdiffeq>=0.2.5
transformers>=4.57.2
pydantic>=2.12.4
omegaconf>=2.3.0
av>=16.1.0
requests
soundfile>=0.13.1
gradio>=6.9.0
hear21passt==0.0.26
git+https://github.com/SonyResearch/Woosh.git@main
