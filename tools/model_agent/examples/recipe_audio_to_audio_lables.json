{
  "model": {
    "id": "example/key-estimation",
    "name": "Key Estimation",
    "description": "Estimate the musical key of an audio clip and return it as a label track.",
    "author": "example",
    "license": "apache-2.0",
    "tags": ["audio-to-audio", "labels"],
    "task": "audio-to-audio"
  },
  "framework": {
    "import": "my_key_model",
    "pip": ["my-key-model", "torch", "torchaudio"],
    "apt": ["ffmpeg"],
    "gpu": false
  },
  "inputs": [
    {
      "name": "input_audio",
      "type": "audio",
      "label": "Input Audio",
      "required": true
    },
    {
      "name": "confidence_threshold",
      "type": "slider",
      "label": "Confidence Threshold",
      "min": 0.0,
      "max": 1.0,
      "step": 0.05,
      "default": 0.5
    }
  ],
  "outputs": [
    { "name": "output_audio", "type": "audio", "label": "Output Audio" },
    { "name": "labels", "type": "labels", "label": "Detected Key" }
  ],
  "inference": {
    "setup": "import shutil\nimport tempfile\n\nfrom my_key_model import estimate_key\n\n\ndef _copy(path):\n    out = tempfile.NamedTemporaryFile(suffix=\".wav\", delete=False)\n    out.close()\n    shutil.copy(path, out.name)\n    return out.name",
    "body": "key, confidence = estimate_key(input_audio)\nlabels = LabelList()\nif confidence >= confidence_threshold:\n    labels.append({\"t\": 0.0, \"label\": key, \"description\": f\"confidence={confidence:.2f}\"})\nreturn _copy(input_audio), labels.to_json()"
  }
}
