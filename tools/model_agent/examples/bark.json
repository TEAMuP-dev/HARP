{
  "framework": {
    "apt": [],
    "gpu": true,
    "import": "bark",
    "pip": [
      "git+https://github.com/suno-ai/bark.git@main",
      "boto3",
      "encodec",
      "funcy",
      "huggingface-hub>=0.14.1",
      "numpy",
      "scipy",
      "tokenizers",
      "torch",
      "tqdm",
      "transformers"
    ]
  },
  "inference": {
    "body": "history_prompt_val = history_prompt if history_prompt != \"None\" else None\n\n# Generate audio from text\naudio_array = generate_audio(\n    text_prompt,\n    history_prompt=history_prompt_val,\n    text_temp=text_temp,\n    waveform_temp=waveform_temp,\n    silent=True, # Disable progress bar for Gradio app\n    output_full=False # Only return the audio array\n)\n\n# Save audio to a temporary WAV file\noutput_file = tempfile.NamedTemporaryFile(suffix=\".wav\", delete=False)\noutput_file.close()\nwrite_wav(output_file.name, SAMPLE_RATE, audio_array)\n\nreturn output_file.name",
    "setup": "from bark import generate_audio, preload_models, SAMPLE_RATE\nfrom scipy.io.wavfile import write as write_wav\nimport tempfile\nimport os\n\n# Download and load all models once\npreload_models()"
  },
  "inputs": [
    {
      "info": "The text to be converted into audio. Bark supports multilingual speech, music, and nonverbal communications.",
      "label": "Text Prompt",
      "name": "text_prompt",
      "required": true,
      "type": "textbox"
    },
    {
      "choices": [
        "None",
        "announcer",
        "en_speaker_0",
        "en_speaker_1",
        "en_speaker_2",
        "en_speaker_3",
        "en_speaker_4",
        "en_speaker_5",
        "en_speaker_6",
        "en_speaker_7",
        "en_speaker_8",
        "en_speaker_9"
      ],
      "default": "en_speaker_0",
      "info": "Choose a speaker to use as a voice prompt for audio cloning. 'None' will use a default voice.",
      "label": "Voice Prompt (Speaker)",
      "name": "history_prompt",
      "type": "dropdown"
    },
    {
      "default": 0.7,
      "info": "Controls the diversity of text token generation (1.0 more diverse, 0.0 more conservative).",
      "label": "Text Generation Temperature",
      "max": 1.0,
      "min": 0.0,
      "name": "text_temp",
      "step": 0.05,
      "type": "slider"
    },
    {
      "default": 0.7,
      "info": "Controls the diversity of waveform token generation (1.0 more diverse, 0.0 more conservative).",
      "label": "Waveform Generation Temperature",
      "max": 1.0,
      "min": 0.0,
      "name": "waveform_temp",
      "step": 0.05,
      "type": "slider"
    }
  ],
  "model": {
    "author": "suno-ai",
    "description": "Bark is a transformer-based text-to-audio model created by Suno. Bark can generate highly realistic, multilingual speech as well as other audio - including music, background noise and simple sound effects. The model can also produce nonverbal communications like laughing, sighing and crying.",
    "id": "suno-ai/bark",
    "license": "MIT",
    "name": "Bark",
    "tags": [
      "text-to-audio",
      "speech-synthesis",
      "multilingual",
      "music-generation"
    ]
  },
  "outputs": [
    {
      "label": "Generated Audio",
      "name": "output_audio",
      "type": "audio"
    }
  ]
}
